"use strict";$(function(){// Handler for .ready() called.
$("#fileUpload").on("change",function(){$("#fileUpload[type=file]").parse({config:{//When a file has completed parsing, it will run the complete function. 
complete:function(a){//Converts 2d Array to json.
csvToJSON(a.data)}},//Executes before the file is parsed
before:function(a){if(console.log(a),!a||".csv"==!a.name.slice(-4))return{action:"abort",reason:"Invalid file, use a CSV file."}},error:function(a,b,c,d){alert(d)}})})});function csvToJSON(a){console.log("Load complete"),word_acro_data={};for(var b=1;b<a.length-1;b++){var c=RegExp.escape(a[b][0]),d=a[b][1].split(",");//TODO, if the spelled out word has a special html char, it will not be 
//imported properly, need to escape
for(var e in d)d[e]=RegExp.escape(d[e]);word_acro_data[c]=d}triggerAnimation()}function triggerAnimation(){M.toast({html:"CSV Uploaded Sucessfully! &#x2713;"})}RegExp.escape=function(a){// 1. let str be ToString(S).
// 2. ReturnIfAbrupt(str).
// 5. For each code point c in cpList in List order, do:
for(var b=(a+"").split(""),c=[],d=0;d<b.length;d++)-1!=="^$\\.*+?()[]{}|".indexOf(b[d])&&c.push("\\"),c.push(b[d]);//6. Let L be a String whose elements are, in order, the elements of cuList.
var e=c.join("");return e};
